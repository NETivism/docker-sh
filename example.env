# DATABASE SETTINGS
# Database name (used by init script as $DB)
INIT_DB=neticrmdb
# Database password (used by init script as $PW)
INIT_PASSWD=password1234

# DOMAIN SETTINGS
# Domain used by drush and init script (used as $DOMAIN)
# Format: domain.name or localhost:8080
INIT_DOMAIN=local.dev.localhost

# SITE SETTINGS
# Site name for initial installation (used by init script as $SITE)
INIT_NAME=netiCRM
# Site email for initial installation (used by init script as $MAIL)
INIT_MAIL=admin@example.com
# Admin account email (used by init script with --account-mail)
HOST_MAIL=admin@example.com

# PORT SETTINGS
# Set the IP address to bind to. Leave blank to bind to all interfaces (0.0.0.0).
# Note: Use separate fields for IP and PORT.
# Do not use IP:PORT format in HTTP_BIND or HTTP_PORT.
HTTP_PORT=8080
# HTTP_BIND=

# NETICRM PATH SETTINGS
# Base path where netiCRM source code is located
# This will be mounted to /mnt/neticrm-{VERSION} in container
NETICRM_BASE_PATH=/mnt/neticrm-10
# netiCRM version (e.g., 7, 10)
NETICRM_VERSION=10

# INITIALIZATION SCRIPT
# Script name for initialization (without .sh extension)
# Available options:
#  - neticrm-10 for Drupal 10
#  - neticrm-7 for Drupal 7
INIT_SCRIPT=neticrm-10
